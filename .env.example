# Text LLM provider — used for question generation + scoring (PRD §8a).
# Default: gemini (see PRD §9).
TEXT_PROVIDER=gemini

# Voice engine provider — used for the realtime speech-to-speech relay (PRD §8b).
# Default: gemini (see PRD §9).
VOICE_PROVIDER=gemini
